# 4 MB flash — two app slots so the firmware can update itself over Wi-Fi.
#
# 1.9 MB per slot leaves comfortable headroom over the current image (~1.3 MB
# including the runway dataset). No SPIFFS partition: the UI font is embedded
# in the binary and nothing else needs a filesystem, so that space is better
# spent on the second OTA slot.
#
# Changing this layout moves the app offset, so the first flash after this
# change has to go over USB — an update cannot repartition the flash it is
# currently running from.
#
# Name,   Type, SubType, Offset,  Size, Flags
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x1F0000,
app1,     app,  ota_1,   0x200000,0x1F0000,
coredump, data, coredump,0x3F0000,0x10000,
